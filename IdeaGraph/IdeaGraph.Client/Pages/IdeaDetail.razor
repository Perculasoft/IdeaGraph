@page "/Idea/{Id}"
@page "/Ideas/{Id}"
@using IdeaGraph.Client.Models
@using IdeaGraph.Client.Services
@using Microsoft.Extensions.Configuration
@inject IdeaService IdeaService
@inject SimilarService SimilarService
@inject SectionService SectionService
@inject TaskService TaskService
@inject KiGateService KiGateService
@inject NavigationManager Navigation
@inject IConfiguration Configuration
@rendermode InteractiveAuto

<PageTitle>@(isCreating ? "New Idea" : idea?.Title ?? "Idea Details") - IdeaGraph</PageTitle>

<div class="container-fluid mt-4">
    @if (isLoading)
    {
        <div class="text-center py-5">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="mt-2">Loading idea...</p>
        </div>
    }
    else if (idea == null && !isCreating)
    {
        <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading">Idea not found</h4>
            <p>The requested idea could not be found.</p>
            <hr>
            <button class="btn btn-primary" @onclick="NavigateToHome">Go to Home</button>
        </div>
    }
    else if (!isEditing)
    {
        <!-- View Mode -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>@idea.Title</h2>
            <div class="d-flex gap-2">
                <button class="btn btn-secondary" @onclick="NavigateToHome">
                    ← Back to List
                </button>
                <button class="btn btn-info" @onclick="ConfirmEnhanceIdea" title="Enhance with AI">
                    🤖 AI Enhance
                </button>
                <button class="btn btn-primary" @onclick="StartEdit">
                    ✏️ Edit
                </button>
            </div>
        </div>

        <div class="card mb-3" style="max-height: 400px; display: flex; flex-direction: column;">
            <div class="card-body" style="overflow-y: auto; flex: 1;">            
                @if (!string.IsNullOrEmpty(idea.Description))
                {
                    <div class="description-content text-white">
                        @((MarkupString)idea.Description)
                    </div>
                }
                else
                {
                    <p class="text-muted">No description provided.</p>
                }
            </div>
        </div>

        <!-- Tabs for Similar and Task -->
        <div class="card mb-3">
            <div class="card-body">
                <ul class="nav nav-tabs" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link @(activeTab == "similar" ? "active" : "")" 
                                @onclick='async () => await SetActiveTab("similar")' 
                                type="button" 
                                role="tab">
                            Similar
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link @(activeTab == "task" ? "active" : "")" 
                                @onclick='async () => await SetActiveTab("task")' 
                                type="button" 
                                role="tab">
                            Task
                        </button>
                    </li>
                </ul>
                <div class="tab-content mt-3">
                    @if (activeTab == "similar")
                    {
                        <div class="tab-pane fade show active" role="tabpanel">
                            @if (isLoadingSimilar)
                            {
                                <div class="text-center py-3">
                                    <div class="spinner-border spinner-border-sm text-primary" role="status">
                                        <span class="visually-hidden">Loading similar ideas...</span>
                                    </div>
                                    <p class="mt-2 mb-0">Finding similar ideas...</p>
                                </div>
                            }
                            else if (similarIdeas.Any())
                            {
                                <div class="list-group">
                                    @foreach (var similarIdea in similarIdeas)
                                    {
                                        var relevanceScore = similarIdea.Distance.HasValue ? 1.0 - similarIdea.Distance.Value : 0.0;
                                        <a href="/Idea/@similarIdea.Id" class="list-group-item list-group-item-action">
                                            <div class="d-flex w-100 justify-content-between">
                                                <h6 class="mb-1">@similarIdea.Title</h6>
                                                <small class="text-muted">@relevanceScore.ToString("P0")</small>
                                            </div>
                                            @if (!string.IsNullOrEmpty(similarIdea.Description))
                                            {
                                                <p class="mb-1 text-muted small">@GetPlainTextPreview(similarIdea.Description, 100)</p>
                                            }
                                        </a>
                                    }
                                </div>
                            }
                            else
                            {
                                <p class="text-muted">No similar ideas found.</p>
                            }
                        </div>
                    }
                    else if (activeTab == "task")
                    {
                        <div class="tab-pane fade show active" role="tabpanel">
                            <!-- Task Management -->
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h5 class="mb-0">Tasks</h5>
                                @if (!isCreating && !isEditingTask && !isCreatingTask)
                                {
                                    <button class="btn btn-sm btn-primary" @onclick="StartCreateTask">
                                        ➕ New Task
                                    </button>
                                }
                            </div>

                            @if (isCreatingTask || isEditingTask)
                            {
                                <!-- Task Edit Form -->
                                <div class="card mb-3">
                                    <div class="card-body">
                                        <h6>@(isCreatingTask ? "Create New Task" : "Edit Task")</h6>
                                        <EditForm Model="@taskEditRequest" OnValidSubmit="@HandleTaskSave">
                                            <DataAnnotationsValidator />
                                            <div class="mb-2">
                                                <label class="form-label">Title</label>
                                                <InputText class="form-control" @bind-Value="taskEditRequest.Title" placeholder="Task title" />
                                            </div>
                                            <div class="mb-2">
                                                <label class="form-label">Description (Markdown)</label>
                                                <InputTextArea class="form-control" rows="4" @bind-Value="taskEditRequest.Description" placeholder="Task description" />
                                            </div>
                                            <div class="mb-2">
                                                <label class="form-label">Repository</label>
                                                <InputText class="form-control" @bind-Value="taskEditRequest.Repository" placeholder="owner/repository" />
                                            </div>
                                            <div class="mb-2">
                                                <label class="form-label">Tags</label>
                                                <div class="tags-input-container">
                                                    <div class="tags-chips">
                                                        @if (taskEditRequest.Tags != null)
                                                        {
                                                            @foreach (var tag in taskEditRequest.Tags)
                                                            {
                                                                <span class="tag-chip">
                                                                    @tag
                                                                    <button type="button" class="tag-remove" @onclick="() => RemoveTaskTag(tag)">×</button>
                                                                </span>
                                                            }
                                                        }
                                                    </div>
                                                    <input type="text" 
                                                           class="form-control mt-2" 
                                                           @bind="currentTaskTag" 
                                                           @onkeydown="HandleTaskTagInput"
                                                           placeholder="Type tag and press Enter or comma" />
                                                </div>
                                            </div>
                                            <div class="mb-2">
                                                <label class="form-label">Status</label>
                                                <InputSelect class="form-select" @bind-Value="taskEditRequest.Status">
                                                    <option value="New">New</option>
                                                    <option value="InWork">In Work</option>
                                                    <option value="Review">Review</option>
                                                    <option value="Ready">Ready</option>
                                                    <option value="Closed">Closed</option>
                                                </InputSelect>
                                            </div>
                                            <div class="d-flex gap-2">
                                                <button type="submit" class="btn btn-sm btn-primary" disabled="@isSavingTask">
                                                    @if (isSavingTask)
                                                    {
                                                        <span class="spinner-border spinner-border-sm me-1"></span>
                                                    }
                                                    Save Task
                                                </button>
                                                <button type="button" class="btn btn-sm btn-secondary" @onclick="CancelTaskEdit">Cancel</button>
                                            </div>
                                        </EditForm>
                                    </div>
                                </div>
                            }

                            @if (isLoadingTasks)
                            {
                                <div class="text-center py-3">
                                    <div class="spinner-border spinner-border-sm text-primary" role="status">
                                        <span class="visually-hidden">Loading tasks...</span>
                                    </div>
                                    <p class="mt-2 mb-0">Loading tasks...</p>
                                </div>
                            }
                            else if (tasks.Any())
                            {
                                <div class="list-group">
                                    @foreach (var task in tasks)
                                    {
                                        <div class="list-group-item">
                                            <div class="d-flex w-100 justify-content-between align-items-start">
                                                <div class="flex-grow-1">
                                                    <h6 class="mb-1">@task.Title</h6>
                                                    @if (!string.IsNullOrEmpty(task.Description))
                                                    {
                                                        <p class="mb-1 text-muted small">@GetPlainTextPreview(task.Description, 150)</p>
                                                    }
                                                    @if (task.Tags.Any())
                                                    {
                                                        <div class="mt-1">
                                                            @foreach (var tag in task.Tags)
                                                            {
                                                                <span class="badge bg-secondary me-1">@tag</span>
                                                            }
                                                        </div>
                                                    }
                                                    @if (!string.IsNullOrEmpty(task.Repository))
                                                    {
                                                        <small class="text-muted">📦 @task.Repository</small>
                                                    }
                                                    @if (!string.IsNullOrEmpty(task.KiSuggestions))
                                                    {
                                                        <div class="mt-1">
                                                            <small class="text-warning">⚠️ AI Suggestions: @task.KiSuggestions</small>
                                                        </div>
                                                    }
                                                </div>
                                                <div class="ms-3">
                                                    <span class="badge @GetTaskStatusBadgeClass(task.Status)">@task.Status</span>
                                                </div>
                                            </div>
                                            <div class="mt-2 d-flex gap-1">
                                                @if (task.Status != "Ready" && task.Status != "Closed")
                                                {
                                                    <button class="btn btn-sm btn-info" @onclick="() => ImproveTask(task.Id)" title="Improve with AI">
                                                        🤖 AI Improve
                                                    </button>
                                                }
                                                @if (task.Status == "Ready" && !string.IsNullOrEmpty(task.Repository))
                                                {
                                                    <button class="btn btn-sm btn-success" @onclick="() => CreateGitHubIssueFromTask(task.Id)" title="Create GitHub Issue">
                                                        📝 Create GitHub Issue
                                                    </button>
                                                }
                                                <button class="btn btn-sm btn-outline-primary" @onclick="() => StartEditTask(task)">Edit</button>
                                                <button class="btn btn-sm btn-outline-danger" @onclick="() => ConfirmDeleteTask(task.Id)">Delete</button>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                            else
                            {
                                <p class="text-muted">No tasks yet. Create your first task to get started.</p>
                            }
                        </div>
                    }
                </div>
            </div>
        </div>

        @if (idea.Tags.Any())
        {
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">Tags</h5>
                    <div>
                        @foreach (var tag in idea.Tags)
                        {
                            <span class="badge bg-secondary me-1">@tag</span>
                        }
                    </div>
                </div>
            </div>
        }

        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">Status</h5>
                <span class="badge @GetStatusBadgeClass(idea.Status)">@idea.Status</span>
            </div>
        </div>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Metadata</h5>
                <p class="mb-0"><strong>Created:</strong> @idea.CreatedAt</p>
                <p class="mb-0"><strong>ID:</strong> @idea.Id</p>
            </div>
        </div>
    }
    else
    {
        <!-- Edit Mode -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>@(isCreating ? "Create New Idea" : "Edit Idea")</h1>
            <div class="d-flex gap-2">
                <button class="btn btn-secondary" @onclick="CancelEdit">
                    Cancel
                </button>
            </div>
        </div>

        <EditForm Model="@editRequest" OnValidSubmit="@HandleUpdate">
            <DataAnnotationsValidator />
            <div class="card mb-3">
                <div class="card-body">
                    <div class="mb-3">
                        <label for="title" class="form-label">Title</label>
                        <InputText id="title" class="form-control" @bind-Value="editRequest.Title" placeholder="Enter idea title" />
                    </div>
                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <Editor Id="description" 
                                @bind-Value="editRequest.Description"
                                Field="@(() => editRequest.Description)"
                                ApiKey="ommjaew9ko96e2kzfsag3kz1fixxl156qbibmaawx9eeqek3"
                                Conf="@editorConf" />
                    </div>
                    <div class="mb-3">
                        <label for="tags" class="form-label">Tags</label>
                        <div class="tags-input-container">
                            <div class="tags-chips">
                                @if (editRequest.Tags != null)
                                {
                                    @foreach (var tag in editRequest.Tags)
                                    {
                                        <span class="tag-chip">
                                            @tag
                                            <button type="button" class="tag-remove" @onclick="() => RemoveTag(tag)">×</button>
                                        </span>
                                    }
                                }
                            </div>
                            <input type="text" 
                                   class="form-control mt-2" 
                                   @bind="currentTag" 
                                   @onkeydown="HandleTagInput"
                                   placeholder="Type tag and press Enter or comma" />
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="section" class="form-label">Section</label>
                        <InputSelect id="section" class="form-select" @bind-Value="editRequest.SectionId">
                            <option value="">-- No Section --</option>
                            @foreach (var sec in sections)
                            {
                                <option value="@sec.Id">@sec.Name</option>
                            }
                        </InputSelect>
                    </div>
                    <div class="mb-3">
                        <label for="status" class="form-label">Status</label>
                        <InputSelect id="status" class="form-select" @bind-Value="editRequest.Status">
                            <option value="New">New</option>
                            <option value="Concept">Concept</option>
                            <option value="Specification">Specification</option>
                            <option value="Ready">Ready</option>
                            <option value="Implemented">Implemented</option>
                            <option value="Discarded">Discarded</option>
                        </InputSelect>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary" disabled="@isUpdating">
                    @if (isUpdating)
                    {
                        <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                        <span>@(isCreating ? "Creating..." : "Updating...")</span>
                    }
                    else
                    {
                        <span>@(isCreating ? "Create Idea" : "Save Changes")</span>
                    }
                </button>
                <button type="button" class="btn btn-secondary" @onclick="CancelEdit">Cancel</button>
            </div>

            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <div class="alert alert-danger mt-3" role="alert">
                    @errorMessage
                </div>
            }
            @if (!string.IsNullOrEmpty(successMessage))
            {
                <div class="alert alert-success mt-3" role="alert">
                    @successMessage
                </div>
            }
        </EditForm>
    }
</div>

<!-- AI Enhancement Confirmation Modal -->
@if (showEnhanceModal)
{
    <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">🤖 AI Enhancement Confirmation</h5>
                    <button type="button" class="btn-close" @onclick="CancelEnhance"></button>
                </div>
                <div class="modal-body">
                    <p>The AI will:</p>
                    <ul>
                        <li>Improve the description (spelling, grammar, formulation)</li>
                        <li><strong>Replace all existing tags</strong> with 5 AI-generated tags</li>
                    </ul>
                    <p class="text-warning"><strong>Warning:</strong> This action will overwrite your current description and tags. This cannot be undone.</p>
                    @if (!string.IsNullOrEmpty(enhanceErrorMessage))
                    {
                        <div class="alert alert-danger" role="alert">
                            @enhanceErrorMessage
                        </div>
                    }
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="CancelEnhance">Cancel</button>
                    <button type="button" class="btn btn-info" @onclick="HandleEnhanceIdea" disabled="@isEnhancing">
                        @if (isEnhancing)
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                            <span>Enhancing...</span>
                        }
                        else
                        {
                            <span>🤖 Enhance with AI</span>
                        }
                    </button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter]
    public string Id { get; set; } = string.Empty;

    private Idea? idea;
    private List<Section> sections = new();
    private bool isLoading = true;
    private bool isEditing = false;
    private bool isCreating = false;
    private bool isUpdating = false;
    private string errorMessage = string.Empty;
    private string successMessage = string.Empty;

    // AI Enhancement state
    private bool showEnhanceModal = false;
    private bool isEnhancing = false;
    private string enhanceErrorMessage = string.Empty;

    // Similar ideas state
    private List<SimilarIdea> similarIdeas = new();
    private bool isLoadingSimilar = false;
    private string activeTab = "similar";
    private double similarityThreshold = 0.8;

    private IdeaUpdateRequest editRequest = new();
    private string currentTag = string.Empty;

    private Dictionary<string, object> editorConf = new Dictionary<string, object>
    {
            {"toolbar", "link | undo redo | bold italic | alignleft aligncenter alignright | bullist numlist | code codesample searchreplace fullscreen emoticons"},
            {"plugins", "lists advlist link image table code charmap autolink codesample link searchreplace fullscreen emoticons"},
            {"height", "525"},
            {"width", "977"},
            {"license_key", "ommjaew9ko96e2kzfsag3kz1fixxl156qbibmaawx9eeqek3"}
    };

    protected override async Task OnInitializedAsync()
    {
        await LoadSections();
        await LoadIdea();
    }

    protected override async Task OnParametersSetAsync()
    {
        // Reset state when navigating to a different idea or creating new
        isEditing = false;
        isCreating = false;
        idea = null;
        editRequest = new();
        currentTag = string.Empty;
        errorMessage = string.Empty;
        successMessage = string.Empty;
        similarIdeas = new();
        activeTab = "similar";
        
        await LoadSections();
        await LoadIdea();
    }

    private async Task LoadSections()
    {
        try
        {
            sections = await SectionService.GetSectionsAsync();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Failed to load sections: {ex.Message}");
            sections = new List<Section>();
        }
    }

    private async Task LoadIdea()
    {
        // If Id is "new", we're creating a new idea
        if (Id.Equals("new", StringComparison.OrdinalIgnoreCase))
        {
            isLoading = false;
            isCreating = true;
            isEditing = true;
            editRequest = new IdeaUpdateRequest
            {
                Title = string.Empty,
                Description = string.Empty,
                Tags = new List<string>(),
                Status = "New",
                SectionId = null
            };
            StateHasChanged();
            return;
        }

        isLoading = true;
        errorMessage = string.Empty;
        StateHasChanged();

        try
        {
            // Load similarity threshold from configuration
            var thresholdValue = Configuration["IdeaGraph:SimilarityThreshold"];
            if (!string.IsNullOrEmpty(thresholdValue) && double.TryParse(thresholdValue, out var threshold))
            {
                similarityThreshold = threshold;
            }

            idea = await IdeaService.GetIdeaAsync(Id);
            
            // Load similar ideas after loading the main idea
            if (idea != null && !isEditing)
            {
                await LoadSimilarIdeas();
                
                // Load tasks if we're on the task tab
                if (activeTab == "task")
                {
                    await LoadTasks();
                }
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to load idea: {ex.Message}";
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task LoadSimilarIdeas()
    {
        isLoadingSimilar = true;
        StateHasChanged();

        try
        {
            var allSimilar = await SimilarService.GetSimilarIdeasAsync(Id, 10);
            
            // Filter by relevance score (distance < (1 - threshold))
            // Distance is typically 0-2, where lower is more similar
            // We want to show ideas with distance < (1 - threshold)
            var distanceThreshold = 1.0 - similarityThreshold;
            similarIdeas = allSimilar
                .Where(s => s.Distance.HasValue && s.Distance.Value < distanceThreshold)
                .ToList();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Failed to load similar ideas: {ex.Message}");
            similarIdeas = new List<SimilarIdea>();
        }
        finally
        {
            isLoadingSimilar = false;
            StateHasChanged();
        }
    }

    private async Task SetActiveTab(string tabName)
    {
        activeTab = tabName;
        
        // Load tasks when task tab is activated
        if (tabName == "task" && !isLoadingTasks && tasks.Count == 0)
        {
            await LoadTasks();
        }
    }

    private string GetPlainTextPreview(string html, int maxLength)
    {
        // Simple HTML tag removal for preview
        var text = System.Text.RegularExpressions.Regex.Replace(html, "<.*?>", string.Empty);
        if (text.Length > maxLength)
        {
            return text.Substring(0, maxLength) + "...";
        }
        return text;
    }

    private void StartEdit()
    {
        if (idea == null) return;

        editRequest = new IdeaUpdateRequest
        {
            Title = idea.Title,
            Description = idea.Description,
            Tags = new List<string>(idea.Tags),
            Status = idea.Status,
            SectionId = idea.SectionId
        };
        isEditing = true;
        errorMessage = string.Empty;
        successMessage = string.Empty;
    }

    private void CancelEdit()
    {
        isEditing = false;
        editRequest = new();
        currentTag = string.Empty;
        errorMessage = string.Empty;
        successMessage = string.Empty;
    }

    private async Task HandleUpdate()
    {
        if (string.IsNullOrWhiteSpace(editRequest.Title))
        {
            errorMessage = "Title is required";
            return;
        }

        isUpdating = true;
        errorMessage = string.Empty;
        successMessage = string.Empty;
        StateHasChanged();

        try
        {
            if (isCreating)
            {
                // Creating a new idea
                var createRequest = new IdeaCreateRequest
                {
                    Title = editRequest.Title ?? string.Empty,
                    Description = editRequest.Description ?? string.Empty,
                    Tags = editRequest.Tags ?? new List<string>(),
                    Status = editRequest.Status ?? "New",
                    SectionId = string.IsNullOrWhiteSpace(editRequest.SectionId) ? null : editRequest.SectionId
                };
                
                var created = await IdeaService.CreateIdeaAsync(createRequest);
                if (created != null)
                {
                    successMessage = "Idea created successfully!";
                    await Task.Delay(1000); // Show success message briefly
                    Navigation.NavigateTo($"/Idea/{created.Id}");
                }
                else
                {
                    errorMessage = "Failed to create idea. Please try again.";
                }
            }
            else
            {
                // Updating existing idea
                var updated = await IdeaService.UpdateIdeaAsync(Id, editRequest);
                if (updated != null)
                {
                    successMessage = "Idea updated successfully!";
                    idea = updated;
                    isEditing = false;
                    await Task.Delay(1000); // Show success message briefly
                    successMessage = string.Empty;
                }
                else
                {
                    errorMessage = "Failed to update idea. Please try again.";
                }
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error {(isCreating ? "creating" : "updating")} idea: {ex.Message}";
        }
        finally
        {
            isUpdating = false;
            StateHasChanged();
        }
    }

    private void HandleTagInput(KeyboardEventArgs e)
    {
        if ((e.Key == "Enter" || e.Key == ",") && !string.IsNullOrWhiteSpace(currentTag))
        {
            AddTag();
        }
    }

    private void AddTag()
    {
        var trimmedTag = currentTag.Trim().TrimEnd(',');
        if (!string.IsNullOrWhiteSpace(trimmedTag))
        {
            if (editRequest.Tags == null)
            {
                editRequest.Tags = new List<string>();
            }
            
            if (!editRequest.Tags.Contains(trimmedTag))
            {
                editRequest.Tags.Add(trimmedTag);
                currentTag = string.Empty;
            }
        }
    }

    private void RemoveTag(string tag)
    {
        if (editRequest.Tags != null)
        {
            editRequest.Tags.Remove(tag);
        }
    }

    private void NavigateToHome()
    {
        Navigation.NavigateTo("/");
    }

    // Status badge helper
    private string GetStatusBadgeClass(string status)
    {
        return status switch
        {
            "New" => "bg-info",
            "Concept" => "bg-primary",
            "Specification" => "bg-warning text-dark",
            "Ready" => "bg-success",
            "Implemented" => "bg-success",
            "Discarded" => "bg-secondary",
            _ => "bg-secondary"
        };
    }

    // AI Enhancement methods
    private void ConfirmEnhanceIdea()
    {
        if (idea == null || string.IsNullOrWhiteSpace(idea.Description))
        {
            errorMessage = "Cannot enhance: Idea has no description.";
            return;
        }
        
        showEnhanceModal = true;
        enhanceErrorMessage = string.Empty;
    }

    private void CancelEnhance()
    {
        showEnhanceModal = false;
        enhanceErrorMessage = string.Empty;
    }

    private async Task HandleEnhanceIdea()
    {
        if (idea == null)
            return;

        isEnhancing = true;
        enhanceErrorMessage = string.Empty;
        StateHasChanged();

        try
        {
            var enhanced = await IdeaService.EnhanceIdeaAsync(Id);
            if (enhanced != null)
            {
                successMessage = "Idea enhanced successfully with AI!";
                idea = enhanced;
                showEnhanceModal = false;
                await Task.Delay(2000); // Show success message
                successMessage = string.Empty;
            }
            else
            {
                enhanceErrorMessage = "Failed to enhance idea. Please try again.";
            }
        }
        catch (Exception ex)
        {
            enhanceErrorMessage = $"Error enhancing idea: {ex.Message}";
        }
        finally
        {
            isEnhancing = false;
            StateHasChanged();
        }
    }

    // Task management state
    private List<IdeaTask> tasks = new();
    private bool isLoadingTasks = false;
    private bool isCreatingTask = false;
    private bool isEditingTask = false;
    private bool isSavingTask = false;
    private string editingTaskId = string.Empty;
    private TaskUpdateRequest taskEditRequest = new();
    private string currentTaskTag = string.Empty;
    private string taskErrorMessage = string.Empty;

    private async Task LoadTasks()
    {
        if (string.IsNullOrEmpty(Id) || Id.Equals("new", StringComparison.OrdinalIgnoreCase))
        {
            return;
        }

        isLoadingTasks = true;
        StateHasChanged();

        try
        {
            tasks = await TaskService.GetTasksByIdeaAsync(Id);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Failed to load tasks: {ex.Message}");
            tasks = new List<IdeaTask>();
        }
        finally
        {
            isLoadingTasks = false;
            StateHasChanged();
        }
    }

    private void StartCreateTask()
    {
        isCreatingTask = true;
        isEditingTask = false;
        editingTaskId = string.Empty;
        taskEditRequest = new TaskUpdateRequest
        {
            Title = string.Empty,
            Description = string.Empty,
            Repository = string.Empty,
            Tags = new List<string>(),
            Status = "New"
        };
        currentTaskTag = string.Empty;
        taskErrorMessage = string.Empty;
    }

    private void StartEditTask(IdeaTask task)
    {
        isEditingTask = true;
        isCreatingTask = false;
        editingTaskId = task.Id;
        taskEditRequest = new TaskUpdateRequest
        {
            Title = task.Title,
            Description = task.Description,
            Repository = task.Repository,
            Tags = new List<string>(task.Tags),
            Status = task.Status
        };
        currentTaskTag = string.Empty;
        taskErrorMessage = string.Empty;
    }

    private void CancelTaskEdit()
    {
        isCreatingTask = false;
        isEditingTask = false;
        editingTaskId = string.Empty;
        taskEditRequest = new();
        currentTaskTag = string.Empty;
        taskErrorMessage = string.Empty;
    }

    private async Task HandleTaskSave()
    {
        if (string.IsNullOrWhiteSpace(taskEditRequest.Title))
        {
            taskErrorMessage = "Title is required";
            return;
        }

        isSavingTask = true;
        taskErrorMessage = string.Empty;
        StateHasChanged();

        try
        {
            if (isCreatingTask)
            {
                var createRequest = new TaskCreateRequest
                {
                    Title = taskEditRequest.Title ?? string.Empty,
                    Description = taskEditRequest.Description ?? string.Empty,
                    Repository = taskEditRequest.Repository ?? string.Empty,
                    Tags = taskEditRequest.Tags ?? new List<string>(),
                    Status = taskEditRequest.Status ?? "New",
                    IdeaId = Id
                };

                var created = await TaskService.CreateTaskAsync(createRequest);
                if (created != null)
                {
                    tasks.Add(created);
                    tasks = tasks.OrderByDescending(t => t.CreatedAt).ToList();
                    CancelTaskEdit();
                }
                else
                {
                    taskErrorMessage = "Failed to create task. Please try again.";
                }
            }
            else if (isEditingTask)
            {
                var updated = await TaskService.UpdateTaskAsync(editingTaskId, taskEditRequest);
                if (updated != null)
                {
                    var index = tasks.FindIndex(t => t.Id == editingTaskId);
                    if (index >= 0)
                    {
                        tasks[index] = updated;
                    }
                    CancelTaskEdit();
                }
                else
                {
                    taskErrorMessage = "Failed to update task. Please try again.";
                }
            }
        }
        catch (Exception ex)
        {
            taskErrorMessage = $"Error saving task: {ex.Message}";
        }
        finally
        {
            isSavingTask = false;
            StateHasChanged();
        }
    }

    private void HandleTaskTagInput(KeyboardEventArgs e)
    {
        if ((e.Key == "Enter" || e.Key == ",") && !string.IsNullOrWhiteSpace(currentTaskTag))
        {
            AddTaskTag();
        }
    }

    private void AddTaskTag()
    {
        var trimmedTag = currentTaskTag.Trim().TrimEnd(',');
        if (!string.IsNullOrWhiteSpace(trimmedTag))
        {
            if (taskEditRequest.Tags == null)
            {
                taskEditRequest.Tags = new List<string>();
            }
            
            if (!taskEditRequest.Tags.Contains(trimmedTag))
            {
                taskEditRequest.Tags.Add(trimmedTag);
                currentTaskTag = string.Empty;
            }
        }
    }

    private void RemoveTaskTag(string tag)
    {
        if (taskEditRequest.Tags != null)
        {
            taskEditRequest.Tags.Remove(tag);
        }
    }

    private async Task ImproveTask(string taskId)
    {
        taskErrorMessage = string.Empty;
        StateHasChanged();

        try
        {
            var improved = await TaskService.ImproveTaskAsync(taskId);
            if (improved != null)
            {
                var index = tasks.FindIndex(t => t.Id == taskId);
                if (index >= 0)
                {
                    tasks[index] = improved;
                }
                successMessage = "Task improved successfully with AI!";
                await Task.Delay(2000);
                successMessage = string.Empty;
            }
            else
            {
                taskErrorMessage = "Failed to improve task. Please try again.";
            }
        }
        catch (Exception ex)
        {
            taskErrorMessage = $"Error improving task: {ex.Message}";
        }
        finally
        {
            StateHasChanged();
        }
    }

    private async Task CreateGitHubIssueFromTask(string taskId)
    {
        taskErrorMessage = string.Empty;
        StateHasChanged();

        try
        {
            var response = await TaskService.CreateGitHubIssueAsync(taskId);
            if (response != null && response.Error == null)
            {
                successMessage = $"GitHub issue #{response.IssueNumber} created successfully!";
                await Task.Delay(3000);
                successMessage = string.Empty;
            }
            else
            {
                taskErrorMessage = $"Failed to create GitHub issue: {response?.Error ?? "Unknown error"}";
            }
        }
        catch (Exception ex)
        {
            taskErrorMessage = $"Error creating GitHub issue: {ex.Message}";
        }
        finally
        {
            StateHasChanged();
        }
    }

    private async Task ConfirmDeleteTask(string taskId)
    {
        // In a real app, you'd show a confirmation dialog
        var success = await TaskService.DeleteTaskAsync(taskId);
        if (success)
        {
            tasks.RemoveAll(t => t.Id == taskId);
            StateHasChanged();
        }
        else
        {
            taskErrorMessage = "Failed to delete task. Please try again.";
        }
    }

    private string GetTaskStatusBadgeClass(string status)
    {
        return status switch
        {
            "New" => "bg-info",
            "InWork" => "bg-primary",
            "Review" => "bg-warning text-dark",
            "Ready" => "bg-success",
            "Closed" => "bg-secondary",
            _ => "bg-secondary"
        };
    }
}
